# Link the MSVC C runtime statically so the exe has no VC++ Redistributable
# dependency (it must run on a machine with nothing installed).
[target.x86_64-pc-windows-msvc]
rustflags = ["-C", "target-feature=+crt-static"]
