# Normalize line endings for all text files to LF in the repo, regardless of
# the checking-out platform or each contributor's local core.autocrlf.
* text=auto eol=lf

# Windows batch files need CRLF to run correctly.
*.bat text eol=crlf

# Binary files: never touch line endings.
*.png binary
*.ico binary
